using;
System;
using;
System.Collections.Generic;
using;
System.Linq;
using;
System.Text;
var BotPal;
(function (BotPal) {
    /// <summary>
    /// </summary>
    class ModifierContext {
    }
    Context;
    {
        DictionaryItem;
        Name;
        ModifierContext(Memory, memory, Concept, concept, DictionaryItem, attributeName, Context, parent = null);
        base(memory, concept, parent);
        {
            Name = attributeName;
        }
        ModifierContext(Concept, concept, DictionaryItem, attributeName, Context, parent = null);
        this(concept.Memory, concept, attributeName, parent);
        {
        }
        // --------------------------------------------------------------------------------------------------------------------
    }
})(BotPal || (BotPal = {}));
//# sourceMappingURL=ModifierContext.js.map