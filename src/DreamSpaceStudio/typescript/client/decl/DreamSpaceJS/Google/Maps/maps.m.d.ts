import { Module } from "../../Scripts";
/** Google maps module. */
export default class extends Module {
    scriptInfo: {
        files: string;
    };
    onBeforeLoad(): void;
    onReady(): void;
}
